<section class="hero">
	<div class="hero-body">
		<h1 class="title">
			<span class="title-light">Welcome to Code Palm Beach</span>
			<br />
			<span class="title-bold">Student Typer</span>
		</h1>
		<p class="subtitle">A tool to help you learn to type, while learning to code HTML and CSS.</p>
	</div>
</section>

<style>
	.hero {
		display: flex;
		align-items: center;
		justify-content: center;
		height: calc(100vh - var(--navbar-height));
	}

	.hero-body {
		text-align: center;
		padding: 2rem;
	}

	.title {
		font-size: var(--size-2);
		color: var(--color-dark);
		line-height: 1.3;
		margin: 0 0 1rem;
	}

	.title-light {
		font-weight: 300;
	}

	.title-bold {
		font-weight: 700;
	}

	.subtitle {
		font-size: var(--size-5);
		font-weight: 700;
		color: var(--color-dark);
		margin: 0;
	}
</style>
